# ── Core web/API ────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
sqlalchemy==2.0.35
psycopg[binary]==3.2.2
python-multipart==0.0.9
 
# ── ML / model ──────────────────────────────────────────────────
torch==2.6.0
sentence-transformers==3.1.1      # used to load BGE-M3 for embeddings
scikit-learn==1.5.2               # baselines, metrics (AUPRC, ROC-AUC, etc.)
numpy==1.26.4
pandas==2.2.3

# ── Data / dataset tooling ──────────────────────────────────────
datasets==3.0.0                   # HuggingFace, for BothBosu/COVA/etc.
datasketch==1.6.5                 # MinHash near-dup detection
presidio-analyzer==2.2.355        # PII detection
presidio-anonymizer==2.2.355
regex==2024.9.11

# ── OCR / ASR (ingestion) ───────────────────────────────────────
easyocr==1.7.2
faster-whisper==1.0.3

# ── Explainability ──────────────────────────────────────────────
captum==0.7.0                     # Integrated Gradients

# ── Misc ────────────────────────────────────────────────────────
python-dotenv==1.0.1
tqdm==4.66.5
pyyaml==6.0.2
httpx==0.27.2                     # threat-intel API calls
